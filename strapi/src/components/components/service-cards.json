{
  "collectionName": "components_components_service_cards",
  "info": {
    "displayName": "Dlaždice",
    "icon": "dashboard"
  },
  "options": {},
  "attributes": {
    "cards": {
      "type": "component",
      "component": "elements.service-card",
      "repeatable": true
    },
    "columns": {
      "type": "enumeration",
      "default": "Three columns",
      "enum": [
        "Two columns",
        "Three columns",
        "Four columns",
        "Five columns"
      ]
    },
    "text_align": {
      "type": "enumeration",
      "default": "Left aligned",
      "enum": ["Left aligned", "Center aligned"]
    },
    "card_clickable": {
      "type": "boolean",
      "default": false
    },
    "background": {
      "type": "enumeration",
      "default": "None",
      "enum": ["None", "Primary light", "Neutral light"]
    }
  },
  "config": {}
}
