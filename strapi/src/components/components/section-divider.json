{
  "collectionName": "components_components_section_dividers",
  "info": {
    "displayName": "Rozdělovník",
    "icon": "minus"
  },
  "options": {},
  "attributes": {
    "spacing": {
      "type": "enumeration",
      "default": "Medium spacing",
      "enum": [
        "Small spacing",
        "Medium spacing",
        "Large spacing"
      ]
    },
    "style": {
      "type": "enumeration",
      "default": "Solid line",
      "enum": [
        "Solid line",
        "Dashed line",
        "Dotted line",
        "Double line",
        "Gradient line"
      ]
    },
    "color": {
      "type": "enumeration",
      "default": "Gray",
      "enum": [
        "Gray",
        "Primary blue"
      ]
    }
  },
  "config": {}
}
