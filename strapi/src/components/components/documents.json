{
  "collectionName": "components_components_documents",
  "info": {
    "displayName": "documents",
    "icon": "folder"
  },
  "options": {},
  "attributes": {
    "documents": {
      "type": "component",
      "component": "elements.document-item",
      "repeatable": true,
      "required": true
    },
    "columns": {
      "type": "enumeration",
      "enum": ["Single column", "Two columns", "Three columns"],
      "default": "Three columns"
    }
  },
  "config": {}
}
